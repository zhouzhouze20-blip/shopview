# 601、603礼品券ODS补数

## 完成结果

2026-09-06补数完成，四项PAPI任务日志均为 `success/completed`。

| 门店 | 本次同步模板 | 本次同步明细 | 源/目标分组核对 | 缺失模板 |
|---|---:|---:|---|---:|
| 601 | 79 | 31,501 | 一致 | 0 |
| 603 | 68 | 239,127 | 一致 | 0 |

明细数量为本次新增或更新的处理范围，不是目标全表行数，也不是本期间新发券量。
核对包含主键、状态、金额汇总以及模板名称、类型、金额和累计计数等字段。
详见[源/目标核对结果](../reports/gift-ods-supplement-20260906/verification.json)和[最终库存及日志](../reports/gift-ods-supplement-20260906/final_inventory.json)。

## 用户确认范围

- 来源：PAPI已登记的BI MySQL（source 14），`ferry_wadge`。
- 目标：ShopView配置使用的PostgreSQL `sales_db.ods`（source 4），不是Mac电脑上另建的数据库。
- 门店601、603，CRM分类 `gift`；不扩大到POS券、停车券或返利券。
- 明细筛选：`create_time`、`update_time`、`used_date_time`任一在 **2026-08-01 00:00:00（含）之后**。
- 这是一段时间内发生变化的券资产集合，不等于这段时间的新发券量。旧券8月之后核销或更新也保留。
- 模板：8月之后建立/更新的礼品券模板，以及入选明细关联的模板；后者可能更早建立。
- 保留删除标记。不得将模板累计发放计数当作本次期间的发放量。

## 目标与运行方式

| 门店 | 模板表 | 明细表 | 一次性PAPI任务 |
|---|---|---|---|
| 601 | `ods.crm_coupon_template_601` | `ods.crm_coupon_record_601` | 49 / 50 |
| 603 | `ods.crm_coupon_template_603` | `ods.crm_coupon_record_603` | 51 / 52 |

601两张表按已核实的603表结构创建，包含BIGINT主键、数值精度、日期字段、非空限制和索引。
按模板ID/唯一券号执行update模式（存在更新，不存在插入），不清空目标表，不删除603已有历史数据。
初次补数阶段全部新任务关闭自动调度，当时未改动43号和45号任务。用户随后授权定时同步，当前状态见下文。

## 验收证据与操作脚本

- [操作脚本](../reports/supplement_gift_ods_20260906.py)：准备、建表/任务、预览、运行、日志、核对分步执行。
- [抽取范围和源计数](../reports/gift-ods-supplement-20260906/source_before.json)
- [任务SQL及字段映射](../reports/gift-ods-supplement-20260906/payloads.json)
- [脱敏预览元数据](../reports/gift-ods-supplement-20260906/previews.json)：不存储客户姓名、手机号或可用券号。

核验覆盖：源记录数、主键唯一性、目标范围记录数、按模板/状态/删除标记分组的笔数和券号汇总、券金额/余额/订单金额/订单用券金额汇总，以及模板字段和关联完整性。
上述金额只用于验证搬运一致性，不代表核准后的商场或供应商应结金额。

603任务发起请求出现55秒客户端等待超时，但服务端任务52持续写入；没有重复启动。最终状态以PAPI任务日志和目标表核验为准。

## 后续活动规划的边界

这次只补数据，不修改现有活动页面、会员归属算法、CRM发券规则、ERP费用规则或供应商结算。
后续应在现有活动全流程规划基础上，分别确认：

1. POS券、礼品兑换、品牌折扣/权益的分类及活动/投放计划归属。
2. 领取人、实际使用人、交易会员及活动时会员等级的关联口径。
3. 直接券号/小票关联与“同日到店消费”关联的区别；后者不直接认定券带来的增量销售。
4. 礼品和品牌权益的成本、承担方及原始结算依据；不从券名中的金额推算应结费用。
5. AI报告读取已核验的指标与证据，先输出审核草稿，不直接发券或结算。

本次礼品券补数完成也不表示POS券模板、会员历史、ERP活动和费用规则已全部进入统一ODS。

## 后续授权：PAPI定时ETL已启用

2026-09-06约09:00（北京时间）启用；所有任务回读 `enabled=1`、`schedule_enabled=1`、`schedule_state=scheduled`，没有调度错误。

| 门店 | 模板同步 | 明细增量 | 每日补偿 |
|---|---|---|---|
| 601 | 49号，每15分钟 | 新增53号，每15分钟回查最近3天变化 | 50号，每日03:30重查8月起范围 |
| 603 | 复用43号，调整为每15分钟 | 复用45号，每15分钟回查最近3天变化 | 52号，每日03:40重查8月起范围 |

仍只覆盖礼品券gift和已确认的8月起范围及关联模板。明细按券号更新，不清空数据。
603原45号SQL中“必须在8月后创建”的限制已替换为创建/更新/核销任一日期符合范围，防止漏掉旧券的新变化。
最近3天重叠回查用于容忍同步延迟；每日补偿进一步覆盖较晚到达、超出3天窗口但在8月起范围内的数据。
模板与明细是独立调度，不能当成跨表事务；短暂缺失关联需等待模板任务完成，报表仍应保留关联完整性检查。
44号旧首载和51号一次性603模板任务保持停用，未重复开启。

调整后模板试跑601为79行、603为68行，增量试跑601为5,398行、603为385行，均success/completed。
每日补偿SQL沿用前一阶段成功补数的SQL，未扩大范围。再次全范围核对31,501/239,127条记录、分组状态和金额及模板字段一致，缺失模板均0。
启用时下一次自动执行为9月6日09:15左右；每日补偿首次执行为9月7日03:30/03:40。这里确认的是调度配置及手动试跑，尚未将首次自动触发称为成功。

证据：[调度回读](../reports/gift-ods-schedule-20260906/status.json)、[完整范围复核](../reports/gift-ods-schedule-20260906/validation.json)、[配置和试跑脚本](../reports/schedule_gift_ods_20260906.py)。
所有作业由PAPI服务器执行，不依赖本机或Codex保持打开；ODS新鲜度仍取决于上游BI MySQL的数据更新。
