# ERP卡券一次性补数验收

2026-09-06约10:07完成补数、10:08完成主要验收。用户明确授权一次性补数，原ETL保留，后续再迁PAPI。

## 结果

| 门店 | 业务日期 | 补前本地笔数 | 补后本地/ERP笔数 | 本地净增加 |
| --- | --- | ---: | ---: | ---: |
| 601 | 2026-08-31 | 149 | 279 | 130 |
| 603 | 2026-08-31 | 69,844 | 79,358 | 9,514 |
| 601 | 2026-09-05 | 846 | 851 | 5 |
| 603 | 2026-09-05 | 413 | 419 | 6 |
| 合计 | 四个范围 | 71,252 | 80,907 | 9,655 |

PAPI任务60，source7富基GPP Oracle → source4销售库 `public.tktcardfqlog`。任务读取/处理80,907条不代表新增80,907条；本地范围净增加9,655条。原ETL未停，因此此净增加不是严格区分每个写入程序的计数。

## 写入边界

- 只处理上述两店、两日；不补其他门店，不扩展到当天。
- 使用 `skip`，唯一键 `tcflseqno`：仅补不存在的流水，已有主键跳过，不覆盖已有记录。
- 不清表、不删除、不修改表结构；ERP/CRM源数据未写入。
- 新任务enabled和schedule_enabled始终为false，手动执行，next_run_at为空。PAPI原有任务配置回读与操作前一致；没有接管或修改外部ETL。
- 复用经核验的33字段映射；金额、流水数字保留精度；目标原有日期字段为date，继续使用业务日期，不把其解释为精确同步时间。

## 验收证据

1. 任务日志52925：success/completed；源读取数、映射数、处理数均80,907。执行约38.6秒，无写入回退记录。处理计数不是新增行数。
2. 四个范围逐组核对笔数、唯一流水数、主键求和及带符号发生金额，差异0。
3. 全部80,907个流水主键逐一比对：ERP独有0、本地独有0，主键集合SHA256相同。不是仅以笔数相等判定完整。
4. 每个范围按流水号倒序抽200条，共800条，33个字段逐项比较均一致。金额按十进制比较，日期按源业务日比较；未声称全量33字段逐行验收。
5. 扩大复查601/603在2026-08-01至2026-09-05的72组日汇总：两侧均280,674条，笔数、唯一流水数、主键求和、带符号金额差异均为0。该扩大范围仅查询，没有额外补写。
6. 客户字段仅在内存完成比对，不在证据中保存或输出会员号、卡号等原始客户记录；全量流水键仅留摘要。

查询均有时间先后，不是跨库原子快照。此次解决已发现历史缺口，不代表原ETL漏数原因已修复，也不代表后续PAPI迁移已完成。活动页面、当前会员接入和活动归属配置未在本轮修改。

证据：[补前](../reports/erp-coupon-backfill-once-20260906/before.json)、[补后](../reports/erp-coupon-backfill-once-20260906/after.json)、[全主键核对](../reports/erp-coupon-backfill-once-20260906/all_key_verification.json)、[800条字段抽样](../reports/erp-coupon-backfill-once-20260906/field_samples.json)、[72组扩大复核](../reports/erp-coupon-backfill-once-20260906/full_period_recheck.json)、[验收汇总](../reports/erp-coupon-backfill-once-20260906/verification.json)、[最终任务状态](../reports/erp-coupon-backfill-once-20260906/status.json)。

执行脚本：[backfill_erp_coupon_once_20260906.py](../reports/backfill_erp_coupon_once_20260906.py)。再次运行会拒绝已有执行日志，防止盲目重复提交；以后补数须重新确认范围和差异，不直接复用这次日期。
