# 部署后用户扫码访问的完整公网地址，不要以 / 结尾。
PUBLIC_BASE_URL=http://gwzxcj.cbxsj.com:8787

# 后台登录口令。正式部署请替换为至少 32 位的随机字符串。
ADMIN_TOKEN=replace-with-a-strong-random-token

# 当前通过域名的 8787 端口直连；启用 HTTPS 反向代理后改为 127.0.0.1。
FIREWORKS_BIND_IP=192.168.98.81
FIREWORKS_PORT=8787
