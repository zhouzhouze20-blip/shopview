# 活动分析模块代办

## 一期口径

- [x] 一期只分析卡券使用，不展开满减、折扣、赠品、银行卡等其他活动规则。
- [x] 暂不建设活动规则明细表，先以 `tktpopinfo` 活动档期和卡券相关字段为准。
- [x] 暂不建设活动归因关系表，先按卡券使用记录、小票记录和活动档期做分析。
- [x] 暂不建设活动成本表，成本和毛利直接取 `salegoodslist`。
- [x] 组织、柜组、部门维度直接使用现有组织柜组部门表。
- [x] 商品、品牌、品类维度直接使用现有商品相关表。
- [x] 会员信息取 `fj_dw_member_dim` 实时信息，暂不做会员分层快照。
- [x] 暂不建设活动前后对比汇总表，第一版先做活动/卡券结果分析。

## 已具备的数据表

- [x] 会员维表：`fj_dw_member_dim`
- [x] 卡券返券日志表：`tktcardfqlog`
- [x] 活动档期表：`tktpopinfo`
- [x] 销售主单：`salehead`
- [x] 销售商品明细：`salegoods`
- [x] 销售付款明细：`salepay`
- [x] 销售单品日汇总：`salegoodslist`
- [x] 组织/柜组/部门表：沿用现有表
- [x] 商品/品牌/品类表：沿用现有表

## 一期重点校验

- [x] 校验卡券日志表 `tktcardfqlog` 中是否同时覆盖发券、用券、冲正/退券等业务动作。
- [x] 梳理 `tktcardfqlog.tcflzy` 的摘要取值，明确哪些代表发券、哪些代表用券、哪些代表冲正。
- [x] 梳理 `tktcardfqlog.tcflsource` 的券来源取值，确认销售返券、买券、退货返券、后台新增等分类。
- [x] 校验 `tktcardfqlog.tcflpopid` 是否能关联 `tktpopinfo.tpiid`：有关联的按活动档期归属；无关联的单独作为非档期发券/用券分析。
- [ ] 校验 `tktcardfqlog.tcflsyjid + tcflinvno + tcflmkt` 是否能关联销售小票。
- [x] 校验 `tktcardfqlog.tcflsyjtrace` 是否能辅助关联小票交易流水：只作为辅助排查字段，不参与主关联条件。
- [x] 校验 `salepay` 中 `paycode = '0500'` 和 `paycode = '0580'` 的付款记录是否都属于卡券使用：两类付款方式名称都是“卡券支付”。
- [ ] 校验 `salepay.payno`、`coupon_trace_seqno`、`coupon_event_id`、`coupon_policy_id` 与 `tktcardfqlog` 的对应关系。
- [x] 校验 `salehead.ybillno`、`salehead.ysyjh`、`salehead.yfphm` 是否能识别退货对应原小票：一期退货冲减先不做原单回溯，直接按 `salegoodslist` 中已计算字段汇总。

### `tktcardfqlog.tcflzy` 摘要字典

| 代码 | 含义 |
| --- | --- |
| `L` | 扣券补现 |
| `m` | 前台买券 |
| `Q` | 后台赠券 |
| `n` | 前台退买券 |
| `w` | 前台买券冲正 |
| `z` | 前台退券冲正 |
| `B` | 券补录 |
| `I` | 后台券新增 |
| `K` | 返券扣回 |
| `M` | 后台买券 |
| `N` | 退买券 |
| `Z` | 支票返券生效 |
| `A` | 券增减 |
| `F` | 返券 |
| `O` | 券消费 |
| `P` | 券退货 |
| `U` | 券消费冲正 |
| `V` | 券退货冲正 |
| `C` | 券转移 |
| `9` | 券延期 |
| `D` | 券作废 |
| `b` | 返订金券 |
| `l` | 扣券补现冲正 |
| `X` | 面值券 |

### 已确认付款口径

- `salepay.paycode = '0500'`：卡券支付。
- `salepay.paycode = '0580'`：卡券支付。
- 活动后台档期表使用 `tktpopinfo`。

### `tktcardfqlog.tcflsource` 来源字典

| 代码 | 含义 |
| --- | --- |
| `1` | 销售返券 |
| `2` | 前台买券 |
| `3` | 银行追送 |
| `4` | 退货返券 |
| `5` | 券转入 |
| `7` | 后台手工新增 |
| `8` | 后台买券 |

### 已确认金额正负口径

- `tcflzy = 'F'` 返券：按正数展示和汇总。
- `tcflzy = 'O'` 券消费：按正数展示和汇总。
- `tcflzy = 'U'` 券消费冲正：抵消券消费，按负数冲减净用券。
- 返券扣回、退货、冲正等其他动作：先按数据库 `tcflmoney` 原始正负汇总，不额外强制转正或转负。

### 已确认关联和经营口径

- 卡券日志关联销售小票的主条件：`tktcardfqlog.tcflmkt + tcflsyjid + tcflinvno` 对 `salehead.mkt + syjh + fphm`。
- `tktcardfqlog.tcflsyjtrace` 只作为辅助排查字段，不参与主关联条件。
- 卡券付款明细对账使用 `salepay.batch = tktcardfqlog.tcflsyjtrace` 精确匹配；否则同一小票上其他活动/非档期券付款会混入活动结果。
- 页面同时展示：
  - 卡券日志发生金额：`tktcardfqlog.tcflmoney`，包含发券、用券、扣回、冲正等日志动作。
  - 实际付款金额：`salepay.je`，仅包含纯付款。
- 经营结果以 `salepay.je` 为准。
- 对同一用券业务，卡券消费日志金额和卡券付款金额理论上应该一致；差异进入数据质量检查。

### 已确认活动归属口径

- `tktcardfqlog.tcflpopid = tktpopinfo.tpiid` 能关联时，归属到对应活动档期。
- 部分券为单独发券，可能不关联活动档期；这类券不强行归入活动档期，单独作为“非档期发券/用券”分析。
- `salepay` 有卡券付款但无法通过卡券日志归属活动时，进入“未归属卡券支付/数据质量”检查。

### 已确认退货冲减口径

- 一期退货冲减直接按 `salegoodslist` 中已计算字段汇总。
- 暂不通过 `salehead.ybillno / ysyjh / yfphm` 回溯原单归因。

### 已确认部门/柜组用券归属口径

- 一般卡券不会跨部门使用，通常是一张小票对应一个柜组。
- 特殊场景主要是超市部：可能一张小票跨柜组，且可能在超市的两个部门之间跨部门使用。
- 部门/柜组分析需要识别跨部门/跨柜组小票，避免把整张小票的卡券付款简单归到单一部门。

## 一期可直接产出的指标

- [ ] 活动档期清单：活动编码、活动主题、活动开始日期、活动结束日期。
- [ ] 卡券发生金额：按活动、日期、门店、会员、券号汇总 `tktcardfqlog.tcflmoney`。
- [ ] 卡券余额变化：按会员、券号、活动看 `tktcardfqlog.tcflye`。
- [ ] 卡券付款金额：按小票、活动、付款方式汇总 `salepay.je`。
- [ ] 0500/0580 付款金额：单独统计两类卡券付款方式。
- [ ] 活动销售额：关联小票后统计 `salehead` / `salegoods` / `salegoodslist` 销售金额。
- [ ] 活动成本和毛利：直接取 `salegoodslist` 中销售成本、毛利、净毛利相关字段。
- [ ] 部门/柜组贡献：通过销售表关联现有组织柜组部门表。
- [ ] 商品/品牌/品类贡献：通过销售表关联现有商品表。
- [ ] 会员贡献：通过会员号关联 `fj_dw_member_dim` 实时会员等级、注册渠道、状态等信息。
- [ ] 退货冲减：通过销售表原小票字段识别退货并冲减活动结果。

## 一期页面/模块代办

- [ ] 活动列表
  - 展示 `tktpopinfo` 活动档期。
  - 支持按活动编码、活动主题、日期筛选。

- [ ] 活动总览
  - 展示销售额、订单数、消费人数、会员人数、卡券付款金额、退货金额、净销售额、毛利、净毛利。
  - [x] 顶部增加本档活动新会员人数、新会员销售额；新会员按会员入会日期落在活动期内统计。
  - 成本和毛利取 `salegoodslist`，不单独维护成本表。

- [ ] 卡券使用分析
  - 展示卡券发放/使用/冲正流水。
  - 同时展示 `tktcardfqlog` 口径和 `salepay paycode in ('0500', '0580')` 口径。
  - 输出两边差异：有卡券日志无付款、有付款无卡券日志、金额不一致。

- [x] 部门/柜组分析
  - 按现有组织柜组部门表汇总销售、用券金额、毛利、净毛利、退货冲减。
  - 输出部门排行和异常部门。

- [ ] 商品/品牌/品类分析
  - 按现有商品表汇总销售、用券金额、毛利、净毛利、退货冲减。
  - 输出品牌、品类、商品排行。

- [ ] 会员分析
  - 使用 `fj_dw_member_dim` 实时会员信息。
  - 统计会员等级、注册渠道、会员状态、年龄段、性别等维度下的活动贡献。

- [ ] 数据质量检查
  - 活动ID无法关联。
  - 卡券日志无法关联小票。
  - 0500/0580付款未匹配卡券流水。
  - 小票无会员号。
  - 销售明细缺失柜组/商品映射。
  - 退货原单无法识别。

## 暂不做事项

- [ ] 暂不建设独立活动规则明细表。
- [ ] 暂不建设活动与销售小票归因关系表。
- [ ] 暂不建设活动成本表。
- [ ] 暂不建设会员分层快照表。
- [ ] 暂不建设活动前后对比汇总表。
- [ ] 暂不做非卡券类活动规则分析。

## 下一步开发顺序

- [x] 第一步：跑样例 SQL，确认 `tktcardfqlog`、`salepay`、`salehead` 三者能否按小票和交易流水关联。
- [x] 第二步：确认 `paycode = '0500'`、`paycode = '0580'` 的付款名称、金额和业务含义。
- [x] 第三步：做卡券使用明细查询接口。
- [x] 第四步：做活动总览汇总接口。
- [x] 第五步：做部门、商品、会员三个拆解接口。
- [x] 第六步：做前端活动分析页面。
- [x] 第七步：补数据质量检查页面或导出。

## 当前开发状态

- [x] 后端支持 `scope=activity|standalone|all`，可分别查询活动档期券、非档期券、全部卡券。
- [x] 后端总览已区分返券日志、用券日志、其他日志、实际卡券付款；经营结果以 `salepay.je` 为准，付款按 `salepay.batch = tcflsyjtrace` 精确归属。
- [x] 后端总览已输出本档新会员人数和新会员销售额，口径为 `fj_dw_member_dim.admission_date` 落在活动开始/结束日期内，销售额取这些会员在本档活动关联小票上的 `salegoodslist.sglxssr`。
- [x] 卡券流水接口已输出 `tcflzy` 中文动作、`tcflsource` 中文来源、原始金额、展示金额、关联小票和付款金额。
- [x] 数据质量接口已支持：卡券日志无法关联小票、付款未匹配卡券流水、日志与付款金额不一致、小票无会员号、无活动档期归属。
- [x] 前端活动分析页已支持活动档期券/非档期券/全部卡券范围切换、总览、付款方式、卡券流水、部门柜组、商品品牌、会员、数据质量页签。
- [x] 部门/柜组分析已按小票内销售额占比分摊卡券付款、返券日志和净用券日志，并输出跨部门/跨柜组小票数。
- [x] 供应商承担金额取 `supgoodspayzkdet.sgpjglsupzk` 字段汇总，页面字段为 `supplier_discount_amount`，不按退损比例反推。

## 目标分析路径

- [ ] 第一步：看本档活动所有卡券发放和使用情况。
  - 发券、用券、退券、扣回、冲正、买券、赠券等动作按 `tcflzy` 汇总。
  - 同时保留流水明细，可追到会员、券号、收银机、小票、交易流水。
- [ ] 第二步：看卡券在各部门的使用、连带销售和卡券退损。
  - 部门维度展示卡券发放、卡券使用、卡券退损、实际卡券付款、销售额、毛利、净毛利、连带销售。
  - 部门数据必须接入现有部门权限/数据范围管理。
- [x] 第三步：选择部门后看对应柜组。
  - 柜组维度继承部门筛选和权限范围。
  - 展示柜组卡券使用、连带销售、毛利；卡券退损待确认业务表后接入。
- [x] 第四步：点击柜组后看对应小票。
  - 小票明细展示小票号、会员、销售时间、销售额、卡券付款、发券/用券流水；退损信息待确认业务表后接入。
  - 小票可继续展开到商品明细和付款明细。

## 待确认术语

- [x] “卡券退损”的业务定义：卡券面额/付款额与实际销售收入确认比例之间的差额。例如卡券 100 元，实际销售收入确认 60%，退损为 40 元。
- [x] 供应商承担金额来源：直接取 `supgoodspayzkdet.sgpjglsupzk`，不使用比例配置推算。
- [ ] 退损承担配置：若后续需要配置比例，只作为业务配置参考，不覆盖 `sgpjglsupzk` 的实际承担金额。
- [ ] 确认退损销售收入确认比例、供应商承担比例的配置来源和粒度。

### 卡券退损计算口径草案

- 卡券金额：以实际卡券付款金额 `salepay.je` 为主。
- 销售收入确认比例：可配置，例如 60%。
- 卡券退损金额：`卡券金额 * (1 - 销售收入确认比例)`。
- 供应商承担金额：直接取 `supgoodspayzkdet.sgpjglsupzk` 汇总。
- 退损后利润调整：`净毛利 + 供应商承担金额`。
- 示例：卡券 100 元，销售收入确认 60%，退损 40 元；供应商承担 50%，供应商承担金额 20 元，利润增加 20 元。
